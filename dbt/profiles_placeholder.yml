# This is a placeholder. Use env var based profiles (do not commit secrets).
# Example (profiles.yml) — keep locally:
#
# analytics_platform:
#   target: dev
#   outputs:
#     dev:
#       type: bigquery
#       method: oauth
#       project: "{{ env_var('GCP_PROJECT') }}"
#       dataset: "{{ env_var('BQ_DATASET') }}"
#       location: "{{ env_var('BQ_LOCATION', 'EU') }}"
#       threads: 4
